import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    padding: 20,
    backgroundColor: "blue",
    borderRadius: 10,
    borderWidth: 1,
    borderColor: "blue",
    fontSize: 15
  }
});
